# Thai Meet Mobile Scaffold

This directory is a placeholder Flutter app shell for Gate 0 route and Trust Loop smoke work.

The first executable mobile smoke should validate the route table in `lib/gate0_routes.dart` before real screens are implemented.
